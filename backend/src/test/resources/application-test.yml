debug: false

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    clean-disabled: true
    validate-migration-naming: true

rating:
  reconciliation:
    enabled: false
    fixed-delay-ms: 5000
